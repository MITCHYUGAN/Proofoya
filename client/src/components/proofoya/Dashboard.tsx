import { AnimatePresence, motion } from "framer-motion";
import { Check } from "lucide-react";
import { dashboardTabs, dashboardViews, type DashboardTab } from "./data";

export function ProjectDashboard({ activeTab, onTabChange }: { activeTab: DashboardTab; onTabChange: (tab: DashboardTab) => void }) {
  const view = dashboardViews[activeTab];
  return <article className="project-dashboard" aria-label="Proofoya projects dashboard mockup"><div className="dashboard-side"><div className="dashboard-logo"><img src="/assets/proofoya-icon.png" alt="" /> PROOFOYA</div><div className="dashboard-tab-list" role="tablist" aria-label="Proofoya dashboard sections">{dashboardTabs.map((tab) => <motion.button layout key={tab} role="tab" aria-selected={activeTab === tab} className={activeTab === tab ? "dashboard-nav active" : "dashboard-nav"} type="button" onClick={() => onTabChange(tab)}>{tab}</motion.button>)}</div><p className="dashboard-side-note">PERMANENT<br />CREATIVE RECORDS</p></div><div className="dashboard-projects"><div className="dashboard-toolbar"><p>{view.label}</p><span className="tool-end" /></div><AnimatePresence mode="wait"><motion.div key={activeTab} className="project-list" initial={{ opacity: 0, y: 7 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -7 }} transition={{ duration: 0.18, ease: [0.23, 1, 0.32, 1] }}>{view.rows.map((item, index) => <div className={index === 0 ? "project-row selected" : "project-row"} key={item.title}><div className="project-thumb" /><div><strong>{item.title}</strong><span>{item.subtitle}</span></div><small>{item.value}</small><em className={item.tone}>{item.status}</em></div>)}</motion.div></AnimatePresence></div><div className="dashboard-detail"><div className="dashboard-toolbar"><p>LIVE DEAL RECORD</p><span className="tool-end" /></div><AnimatePresence mode="wait"><motion.div key={activeTab} initial={{ opacity: 0, y: 7 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -7 }} transition={{ duration: 0.18, ease: [0.23, 1, 0.32, 1] }}><div className="detail-brief"><span>{view.detailEyebrow}</span><h4>{view.detailTitle}</h4><p>{view.detailCopy}</p></div><div className="detail-statuses">{view.details.map((detail) => <p key={detail.label}><span>{detail.label}</span><b>{detail.value}</b><i className={detail.tone} /></p>)}</div></motion.div></AnimatePresence><div className="dashboard-verification"><Check size={11} /> STORED WITH PROOFOYA</div></div></article>;
}
